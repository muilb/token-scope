{
  "name": "tokenscope-central-server",
  "version": "0.1.0",
  "description": "Internal LAN server that ingests aggregated token/cost usage from CodeTokens Monitor extensions and shows a combined member x project dashboard.",
  "private": true,
  "license": "MIT",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "fastify": "^4.28.0"
  }
}
